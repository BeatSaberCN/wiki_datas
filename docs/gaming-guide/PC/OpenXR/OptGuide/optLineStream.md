# 优化有线串流

有线串流是指通过USB数据线进行的串流，不依赖有线网卡。

可以检查这些事项：

- [ ] 检查游戏过程中的GPU内存开销，确保不要占满GPU内存。
- [ ] 绕过/卸载SteamVR，可以节省约2GB的GPU内存，同时降低游戏延迟。
- [ ] 笔记本尝试关闭独显直连，并对比游戏性能。让核心显卡参与到串流中，会降低独立显卡的显存/计算压力。
- [ ] 保证USB数据线速率能达到5Gbps，最好有10Gbps。检查线缆和端口是否满足性能需求。
- [ ] 使用一条TypeC光纤线，而非普通铜线。光纤线会更轻，但这并不意味着会有更强的串流性能。
